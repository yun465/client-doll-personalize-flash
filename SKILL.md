---
name: client-doll-personalize-flash
description: "Use when personalizing, preparing, building, provisioning, or verifying an ESP32-S3 AI doll for one customer while handling customer-provided chat or explicitly authorized local audio."
---

# 客户玩偶个性化与烧录

为一位客户生成隔离的人格配置和可验证的 ESP32-S3 构建或烧录结果。把聊天、录音、凭据、设备备份和包含私人音频的固件始终留在客户私有目录。

## 适用范围与项目依赖

- 只进行聊天分析和脱敏人格预览时，不要求固件工程存在。
- 集成、构建或烧录依赖目标项目提供聊天分析、人格编译、公共配置写入、构建、板卡探测和 Flash 验证能力。缺少对应能力时停在上一个可完成阶段，不临时编造替代实现。
- 进入集成前阅读 [project-adaptation.md](references/project-adaptation.md)，从目标项目和用户提供的信息解析真实路径、工具链与硬件参数。
- 只有目标项目明确匹配仓库内参考配置时，才使用 [current-project-baseline.md](references/current-project-baseline.md)。目标项目自己的受版本控制配置和构建产物优先。
- 若项目根目录存在 `AGENTS.md`、`PLAN.md` 或等价门禁文件，进入构建或烧录阶段前读取并遵守。

## 授权门禁

把下面各阶段视为独立授权范围；用户对前一阶段的批准不自动扩大到后一阶段：

| 阶段 | 默认允许的动作 | 进入下一阶段所需条件 |
|---|---|---|
| 人格分析 | 本地解析、脱敏统计、离线预览 | 用户明确验收人格与回复风格 |
| 本地唤醒录音 | 本地检查、裁剪、转换和离线验证 | 声音权利人授权，并明确允许构建 |
| 构建 | 离线测试与构建，不连接设备 | 用户允许只读识别目标板 |
| 板卡识别 | 枚举串口、读取芯片与 Flash 身份 | 用户确认目标板和精确写入范围 |
| 烧录 | 只写批准的分区并读回验证 | 另行批准秘密配置或真实云端测试 |
| 真实云端测试 | 恰好执行批准的受限调用 | 不自动延续到第二次调用 |

不得把“继续”“帮我完成”或已提供录音解释为擦除、烧录、凭据写入、声音克隆、音频上传或付费 API 调用授权。

## 必需输入

只询问当前阶段缺少的信息；用户已经提供的不要重复询问：

- 聊天文件的精确本地路径。
- 允许学习风格的说话人精确标签。
- 对话对象、称呼方式和期望回复效果。
- 本轮允许完成到哪个阶段。
- 若处理唤醒录音：声音权利人、明确授权和文件路径。
- 若识别或烧录：目标板信息、新板或已有客户板，以及批准的写入范围。

说话人无法稳定区分时，只报告识别到的标签和数量并等待用户指定，不猜测目标人物。

## 工作流

### 1. 建立客户隔离区

- 每位客户使用独立目录，例如 `customers/<customer-id>/`；`customer-id` 不使用真实姓名。
- 原始聊天和录音只放在该客户的 `private_data/`，先确认它被 Git 忽略再处理。
- 从母版创建工作副本时只复制受版本控制文件，不复制其他客户目录、构建缓存、备份或密钥。
- 只记录母版提交、客户代号和生成时间，不记录聊天正文或真实身份。

### 2. 生成并验收人格

按 [persona-workflow.md](references/persona-workflow.md)执行。

- 只使用用户指定说话人的消息推断风格；其他人的消息只提供情境。
- 用户本次明确要求高于聊天统计，偏离处标记为“主动定制”。
- 排除秘密、第三人隐私、医疗隐私、详细地址、争吵气话、操控和贬低表达。
- 不把原始聊天嵌入固件，只生成简短人格摘要、经批准示例、确认记忆和禁用主题。
- 明确说明玩偶是基于表达习惯设计的 AI 角色，不冒充现实中的本人。

展示脱敏审批预览。用户明确确认人格和回复风格前，不处理录音、不集成、不构建，也不连接设备。

### 3. 导入本地唤醒回应

人格通过后，才按 [wake-ack-audio.md](references/wake-ack-audio.md)读取本人或已授权的短录音。

- 录音默认只用于设备本地唤醒回应，不上传、不创建云端音色，也不代表批准 ASR、LLM 或 TTS。
- 原始文件保持原处；派生 WAV、PCM 和包含录音的应用镜像是客户私有产物。
- 音频不合格时请求重录，不使用声音克隆、变声或云端修复绕过质量问题。

### 4. 集成与构建

- 使用 [project-adaptation.md](references/project-adaptation.md)中确认的项目适配器。目标项目没有适配器时，交付脱敏人格包并报告缺失能力，停止在烧录之前。
- 公共人格与秘密配置分离：人格进入项目的公共配置通道；Wi-Fi、API Key 和个人音色 URI 只进入项目定义的秘密配置流程。
- 使用目标项目锁定的工具链、硬件配置、分区表、唤醒模型和测试命令。仅在目标项目匹配参考配置时，才按 [firmware-and-flash.md](references/firmware-and-flash.md)中的具体参数构建。
- 先运行人格、提示词、隐私和音频定向测试，再完整构建。日志不得出现聊天正文、密钥、密码、音色 URI 或录音内容。
- 报告应用大小、分区余量、模型清单、镜像 SHA-256、测试结果和未完成风险，然后等待板卡识别或烧录授权。

### 5. 识别、烧录与验证

- 烧录前重新枚举设备并只读确认芯片、MAC、Flash、PSRAM、分区表和必需分区完整性。
- 展示目标板身份，以及每个拟写分区的名称、偏移、文件和长度。目标项目生成的 Flash 参数优先，不使用历史 COM 号、MAC、镜像路径或参考项目偏移替代现场数据。
- 新板只有在明确批准完整烧录后才能擦除厂家固件；已有客户板默认最小写入并保留 NVS、人格槽、模型和其他客户数据。
- 不烧写 Flash Encryption、Secure Boot、JTAG 禁用或密钥相关 eFuse，除非目标项目另有经过审核的安全方案且用户明确要求；本参考配置没有该方案。
- 每段写入后读回或核对哈希，再验证启动、Flash/PSRAM、文件系统、人格和唤醒模型。
- 未获真实云端测试批准时，验证停在本地路径。只查询秘密“存在/不存在”状态，不读取或回显秘密值。

## 运行故障

命令运行器未启动不等于文件不存在。若环境提供安全且已获允许的重试方式，可对同一条最小只读命令重试一次；否则停止并报告原始运行错误。恢复后先核对当前目录和精确路径，再继续当前阶段。运行恢复不扩大聊天读取、联网、烧录或付费操作的授权。

## 完成报告

最终报告包含：

- 客户工作副本和脱敏人格产物位置。
- 目标说话人标签、纳入/排除统计，以及用户主动定制项。
- 人格验收状态；若处理录音，列出授权、格式、时长、首尾静音和哈希。
- 测试、构建、板卡识别、写入分区、镜像哈希及启动证据。
- 明确列出未执行的阶段，尤其是音频上传、声音克隆、秘密写入、eFuse 和真实 API 调用。
- 确认原始聊天、录音、密钥、音色 URI 和设备备份未进入 Git 或 Skill。
